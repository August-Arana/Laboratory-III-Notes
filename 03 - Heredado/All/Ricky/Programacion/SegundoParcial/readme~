
inEnviar_mensaje(id_cola_mensajes, MSG_DESTINO, MSG_REMITENTE, EVT, cadena);
inRecibir_mensaje(id_cola_mensajes, MSG_DESTINO, &msg);

USAR FLAGS MEMORIA PARA ARRANCAR PROCESOS
USAR MEMORIA PARA DATOS QUE TENGAN QUE COMPARTIR LOS DOS PROCESOS (acordate de simulacro)
LIBERA MEMORIA DINAMICA !!
ACORDATE DE CAMBIAR MAKEFILE
EL THREAD .H EDITALO!!

sprintf(cadena, "%d %d", int1, int2); -> guarda valor de int1 e int2 en cadena
sscanf(cadena, "%d %d", int1, int2); -> descompone cadena y fuarda valores en int1 e int2

/*COPIAR CADENA DE CARACTERES*/ -> argv se copia en ciudad1
strcpy(ciudad1, argv[1]);
NO GUARDES CADENAS DE CARACTERES CON =



#ifndef _ARCHIVOS_H
#define _ARCHIVOS_H

int inAbrirArchivo(char *);
int inEscribirArchivo(char *);

#endif

/*
 * Obviamente, si no usa el framework, estas desaprobado
 * Pensar en otras funciones que consideremos utiles
 * */
